import React from 'react';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    'main-container': {
        margin: '10%',
        width: '100%',
        height: '100%'
    }
});
