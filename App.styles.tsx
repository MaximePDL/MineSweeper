import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainContainer: {
        display: 'flex',
        flex: 1,
        alignItems: 'center',
        flexDirection: 'column',
    },
});
